"""Capa de abstraccion de conexion a IA (seccion 20 del requerimiento).

El resto de VISTA usa `conexion_service.adaptador_texto(db)` /
`adaptador_voz(db)` y nunca importa el SDK de un proveedor.
"""
